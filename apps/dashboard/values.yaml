web:
  service:
    type: LoadBalancer
    extraSpec:
      loadBalancerIP: "192.168.1.251"
app:
  security:
    csrfKey: null

api:
  containers:
    args:
      - --enable-skip-login
      - --disable-settings-authorizer
